import { describe, it, expect } from 'vitest';

// WU1 tooling smoke test — proves the Vitest runner is wired up.
// Replaced by real unit/integration tests in WU3+.
describe('backend test runner', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
